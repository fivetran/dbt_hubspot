version: 2

models:
  - name: int_hubspot__email_aggregate_clicks
    columns:
      - name: email_send_id
        tests:
          - unique
          - not_null

  - name: int_hubspot__email_aggregate_opens
    columns:
      - name: email_send_id
        tests:
          - unique
          - not_null